const auth_domain = require("../domain/auth_domain");


// test('check a proper login of a user, positive test', () => {
//     expect(auth_domain.loginUser("ronit", "lady@16").not.toEqual(null))
// })

test('check', () => {
    let x = 1
    expect(x.toEqual(1))
})